# Refactoring Tasks: Knit -> SwarSetu

- `[x]` Move package source files to `app.swarsetu` package structure (main, test, androidTest, debug, release)
- `[x]` Update Kotlin/Java file package declarations and imports from `app.getknit.knit` to `app.swarsetu`
- `[x]` Rename core classes, interfaces, and methods:
    - `KnitApplication` -> `SwarSetuApplication`
    - `KnitDatabase` -> `MeshDatabase`
    - `KnitMigrations` -> `MeshMigrations`
    - `KnitApp` -> `SwarSetuApp`
    - `KnitStitchIndicator` -> `StitchIndicator`
- `[x]` Update package names and names in:
    - `app/build.gradle.kts` (namespace, applicationId, etc.)
    - `settings.gradle.kts`
    - `AndroidManifest.xml` (main & debug)
    - `swarsetu-r8.keep` (renamed from `knit-r8.keep`)
    - `ic_swarsetu_room.xml` (renamed from `ic_knit_room.xml`)
- `[x]` Rename Room database schema directories from `app.getknit.knit.data.KnitDatabase` to `app.swarsetu.data.MeshDatabase`
- `[x]` Replace user-facing branding and references in `strings.xml` and `themes.xml`
- `[x]` Update README.md and documentation
- `[x]` Verify Gradle build, compile, and tests
- `[x]` regression search and finalize validation report
